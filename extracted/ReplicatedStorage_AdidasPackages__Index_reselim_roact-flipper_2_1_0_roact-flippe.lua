-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v0 = require(script.getBinding);
local v1 = require(script.useMotor);
local v2 = require(script.useGoal);
local v3 = require(script.useInstant);
local v4 = require(script.useSpring);
local v5 = require(script.useLinear);
return {
    getBinding = v0, 
    useMotor = v1, 
    useGoal = v2, 
    useInstant = v3, 
    useSpring = v4, 
    useLinear = v5
};